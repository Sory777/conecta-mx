{
  "name": "gps-device-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Servidor TCP que habla el protocolo GT06 (usado por trackers GPS/GSM baratos) y reenvia las ubicaciones a la Edge Function ingest-position.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
